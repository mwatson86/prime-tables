{
  "name": "prime-tables",
  "version": "1.0.0",
  "description": "identify prime numbers",
  "main": "package",
  "scripts": {
    "dev": "NODE_ENV=development NODE_PATH=src nodemon --watch src --exec 'babel-node src'",
    "start": "NODE_ENV=production NODE_PATH=package node package",
    "build": "babel src --out-dir package",
    "test": "NODE_ENV=test NODE_PATH=src mocha --require babel-register"
  },
  "author": "Miles Watson",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5",
    "supertest": "^3.1.0"
  },
  "dependencies": {
    "express": "^4.16.3",
    "pug": "^2.0.3"
  }
}
